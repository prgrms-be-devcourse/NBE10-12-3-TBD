# 📋 발표 후 예상 질문 & 답변 모음

> 빠르게 찾아볼 수 있도록 카테고리별로 정리했습니다.
> 실제 수치(커버리지, 부하테스트 결과 등)는 발표 전 팀에서 직접 채워주세요.

---

## 목차
- [기획](#-기획) — Q1~Q6
- [설계](#-설계) — Q7~Q14
- [개발](#-개발) — Q15~Q23
- [인증 / 인가](#-인증--인가) — Q24~Q40 ← 로그인·로그아웃 흐름 포함
- [배포](#-배포) — Q41~Q53 ← EC2 세팅 포함
- [테스트 / 품질](#-테스트--품질) — Q54~Q58
- [구조 관련 심화 질문](#-구조-관련-심화-질문) — Q59~Q68
- [코드 기반 심화 질문](#-코드-기반-심화-질문) — Q69~Q76

---

## 📌 기획

---

### Q1. 오늘뭐먹지만의 차별점은 무엇인가요? 기존 망고플레이트, 식신 같은 서비스와 무엇이 다른가요?

> **A.**
> 광고성 리뷰 대신 **팔로우 기반 신뢰 리뷰**를 노출합니다.
> 평점·거리 중심이 아닌 **상황별(혼밥·데이트·회식) 맞춤 추천**과
> **테마별 큐레이션 리스트 공유**를 핵심 차별점으로 설계했습니다.

---

### Q2. 타겟을 20~30대로 좁힌 이유는 무엇인가요?

> **A.**
> SNS 기반 정보 탐색에 익숙하고,
> **광고성 리뷰에 대한 피로도와 불신이 특히 높은 세대**라는 점을
> 서비스 방향성 설정의 근거로 삼았습니다.

---

### Q3. MVP 범위는 어떻게 정했나요? 제외하거나 축소한 기능이 있나요?

> **A.**
> 멘토링 피드백에 따라 레스토랑 태그, 리스트 복사 기능은 **최소 범위만 구현**했습니다.
> 알림 기능은 당초 복잡도를 이유로 제외 예정이었으나, 스프린트 중 여유가 생겨 **조기 구현**했습니다.
> 발표자료 업데이트가 늦어진 부분이며, 실제로는 Redis Streams 기반 알림 파이프라인까지 완성된 상태입니다.

---

### Q4. 향후 로드맵(개인화 추천, DM, 모바일 앱) 중 우선순위는 어떻게 정했나요?

> **A.**
> 1. **개인화 추천 고도화** — 사용자 리텐션에 직접적 영향
> 2. **DM** — 소통 강화
> 3. **모바일 앱** — 접근성 확장
>
> 서비스 핵심 가치에 직접적으로 기여하는 순서로 우선순위를 정했습니다.

---

### Q5. 리텐션을 높이기 위한 전략은 무엇인가요?

> **A.**
> 현재는 **팔로우 기반 피드**와 **테마별 큐레이션 리스트**로 1차 리텐션을 유도하고 있습니다.
> 로드맵상 개인화 추천이 고도화되면, 좋아요·저장 등 **사용자 행동 데이터를 활용해 재방문을 유도**할 계획입니다.

---

### Q6. 후발주자로서 대형 서비스(망고플레이트 등) 대비 어떤 진입 전략을 취했나요?

> **A.**
> 대형 서비스가 다루지 않는 **"신뢰 기반 소셜 큐레이션"이라는 좁은 영역에 집중**했습니다.
> 3주라는 짧은 스프린트 안에서는 범위를 넓히기보다
> **회원가입부터 추천까지 핵심 플로우를 빠르게 검증**하는 데 집중했습니다.

---

## 📐 설계

---

### Q7. ERD는 왜 이렇게 구성했나요? users를 중심으로 설계한 이유는?

> **A.**
> 사용자 인증 도메인이 **모든 도메인의 접근을 제어**하는 구조이기 때문입니다.
> `users` 테이블이 피드·팔로우·리스트 등 모든 관계의 시작점이 되도록 설계했습니다.

---

### Q8. 좋아요, 댓글, 팔로우를 각각 별도 테이블로 분리한 이유는?

> **A.**
> 다대다 관계를 정규화하고,
> **이후 알림·통계 기능이 추가되더라도 기존 테이블 구조를 변경하지 않고 확장**할 수 있도록 하기 위함입니다.

---

### Q9. 맛집 리스트의 아이템 순서는 어떻게 관리하나요?

> **A.**
> 순서를 지정하지 않으면 **자동으로 맨 뒤에 배치**되고 (`maxOrderIndex + 1`),
> 중간에 삽입하면 `incOrderIndex()`로 **이후 항목들의 순서 인덱스가 자동으로 밀려**나도록 구현했습니다.

---

### Q10. API 응답 형식을 `{status, message, data}`로 통일한 이유는?

> **A.**
> 프론트엔드가 API마다 다른 형식을 파싱할 필요 없이,
> **하나의 공통 로직으로 성공·실패를 판별하고 에러를 처리**할 수 있도록
> 일관성을 확보하기 위함입니다.

---

### Q11. 도메인을 5개로 나눈 기준은 무엇인가요?

> **A.**
> 인증 / 식당 탐색·추천 / 소셜 피드 / 맛집 리스트 / 프로필로 나눴습니다.
> **책임 범위와 데이터가 흐르는 방향**
> (인증이 접근을 제어 → 탐색이 피드·리스트에 데이터를 공급)을 기준으로 분리했습니다.

---

### Q12. `RsData` 같은 공통 응답 래퍼는 실제로 어떻게 구현되어 있나요?

> **A.**
> `RsData<T>`는 `success`, `data`, `message` 세 필드를 가진 record 타입으로,
> 모든 컨트롤러가 `RsData.success(data, message)` 형태로 응답을 감쌉니다.
> `GlobalExceptionHandler`가 예외 발생 시에도 동일한 `ErrorResponse` 포맷의 실패 응답으로 변환해
> 프론트가 항상 같은 구조로 파싱할 수 있게 했습니다.
>
> #### └ 꼬리질문. 400, 401, 404 같은 에러 상태코드는 어디서 결정되나요?
>
> **A.**
> `GlobalExceptionHandler`에서 예외 타입별로 HTTP status를 매핑합니다.
> 예를 들어 `InvalidCredentialsException` → 401,
> `MethodArgumentNotValidException` → 400,
> `UserNotFoundException` → 404,
> `AlreadyLikedFeedException` → 409로 매핑되고,
> 각 상황에 맞는 메시지가 `ErrorResponse.message`에 함께 담깁니다.

---

### Q13. `restaurant_list`와 `restaurant_list_item`을 왜 별도 테이블로 분리했나요?

> **A.**
> 하나의 리스트에 여러 식당이 **순서를 가지고 담기는 다대다 구조**이기 때문입니다.
> 리스트 자체의 메타데이터(제목·설명·무드태그)와
> 아이템의 순서 정보(`orderIndex`)·메모를 **독립적으로 관리**하도록 분리했습니다.

---

### Q14. 도메인 간 순환 참조는 없나요? 특히 알림이 피드를 참조하는 구조에서요.

> **A.**
> 알림 도메인이 피드 도메인의 이벤트를 구독하긴 하지만,
> 엔티티끼리 직접 참조하는 대신
> **`Redis Stream`이라는 메시지 계층을 매개로 느슨하게 연결**했습니다.
> `FeedStreamPublisher` → `notification:feed-created` Stream → `FeedCreatedStreamListener`
> 순서로 흐르기 때문에 도메인 간 강결합과 순환 참조를 피했습니다.

---

## ⚙️ 개발

---

### Q15. 좋아요 기능의 동시성 문제는 어떻게 처리했나요?

> **A.**
> 우선 **기본 기능 구현을 먼저** 완료했습니다.
> 향후 **메시지 큐에 요청을 담아 순차적으로 처리**하는 방식으로 동시성 이슈를 해결할 계획입니다.
> 처리 단위 자체도 게시글 ID와 사용자 ID만 받아 카운트를 +1 하는 단순한 구조로 설계했습니다.

---

### Q16. DB 부하를 줄이기 위해 어떤 기법을 적용했나요?

> **A.**
> 우선순위에 따라 아래 기법을 적용(또는 적용 예정)했습니다.
> - **Caffeine 캐시** — 자주 조회되는 데이터 캐싱
> - **JPA Pageable 기반 페이징** — 대량 데이터 한꺼번에 로드 방지
> - **인덱싱** — 자주 조회되는 컬럼에 적용
> - **Bucket4j / Guava 기반 Rate Limiter** — 요청 과부하 방지

---

### Q17. 추천 피드 로직은 어떻게 구현되어 있나요?

> **A.**
> **본인과 이미 팔로잉 중인 사용자의 피드를 제외**한 뒤,
> 남은 피드를 **Pageable로 페이징해 노출**하는 방식으로 구현했습니다.
> 팔로잉하는 유저가 없는 신규 사용자에게는 전체 피드를 폴백으로 보여줍니다.

---

### Q18. 카카오맵 API 연동에서 어려웠던 점은 무엇인가요?

> **A.**
> 초기 데이터셋을 수천 건 이상 확보해두고,
> **검색·추천 결과로 실제 노출된 식당만 선택적으로 레스토랑 테이블에 저장**하는 전략을 통해
> 불필요한 데이터 적재를 줄이는 부분이 관건이었습니다.
> (`saveGetKakao` — 이미 DB에 있으면 재사용, 없으면 새로 저장하는 upsert 전략)

---

### Q19. 테스트는 어떤 기준으로 작성했나요? (Controller / Service 테스트 구분 이유)

> **A.**
> - **컨트롤러 테스트** — 서비스 계층을 Mock 처리하고 HTTP 요청·응답 검증에 집중
> - **서비스 테스트** — Repository 등 외부 의존성만 Mock 처리해 순수 비즈니스 로직 검증
>
> 관심사를 분리해 각 계층이 해야 할 역할만 테스트합니다.

---

### Q20. 알림 기능은 Redis Streams로 정확히 어떻게 동작하나요?

> **A.**
> 1. 피드가 생성되면 트랜잭션 커밋 이후 `FeedEventListener`가 이벤트를 받아
> 2. `FeedStreamPublisher`가 `notification:feed-created` 스트림에 `feedId`·`authorId`를 기록합니다.
> 3. `notification-group` 컨슈머 그룹의 `FeedCreatedStreamListener`가 이를 구독해
> 4. `NotificationService.createFeedNotifications()`를 호출하고
> 5. 작성자의 팔로워 전체에게 알림 데이터를 DB에 저장합니다.
>
> #### └ 꼬리질문 1. 발표자료에는 알림이 "향후 계획"으로 적혀 있던데, 실제로는 왜 이미 구현되어 있나요?
>
> **A.**
> 스프린트 중 시간 여유가 생겨 로드맵으로 잡아두었던 알림 기능을 **조기 구현**했는데,
> 발표자료 업데이트가 누락된 부분입니다.
> 실제로는 이벤트 기반 알림 파이프라인까지 구현이 끝난 상태이며, 발표 시 이 점을 정정할 예정입니다.
>
> #### └ 꼬리질문 2. Redis가 재시작되거나 잠깐 끊기면 컨슈머는 어떻게 되나요?
>
> **A.**
> `NotificationStreamInitializer`에 **데몬 스레드로 5초 간격 재구독 로직**을 넣어서,
> Redis 연결이 일시적으로 끊겨도 애플리케이션이 죽지 않고
> 재시도하며 자동 복구되도록 처리했습니다.

---

### Q21. `recommendation` 도메인은 왜 비어 있나요? restaurant의 추천 API와는 어떻게 다른가요?

> **A.**
> 현재 "추천" 기능은 `RestaurantController`의 **규칙 기반(카테고리·지역 조건) API**로 구현되어 있고,
> `recommendation` 패키지는 향후 **좋아요·저장 로그를 활용한 협업 필터링이나 개인화 추천 로직**을 위해
> 미리 잡아둔 확장 자리로 아직 구현되지 않았습니다.

---

### Q22. 더미 데이터는 어떻게 자동으로 채워지나요?

> **A.**
> `UserDummyInitializer`, `FeedDummyInitializer`, `RestaurantDummyInitializer`가
> 애플리케이션 구동 시점(`@PostConstruct` 또는 `CommandLineRunner`)에 실행되면서
> **테스트·시연에 필요한 유저, 피드, 식당 데이터를 자동으로 시딩**합니다.

---

### Q23. 좋아요 수 동시성 문제는 지금 코드 기준으로 실제로 어디서 발생할 수 있나요?

> **A.**
> `FeedLikeService.like()`에서 `feed.increaseLikeCount()`를 단순 +1 하는 구조라,
> 여러 요청이 동시에 같은 피드에 좋아요를 눌렀을 때 **갱신 유실(Lost Update)** 이 발생할 수 있습니다.
> 향후 좋아요 요청을 **메시지 큐에 적재하고 순차 처리**하는 방식으로 개선할 계획입니다.

---

## 🔐 인증 / 인가

---

### Q24. 인증/인가는 어떻게 구현했나요?

> **A.**
> **JWT 기반 인증**에 **카카오 OAuth2 소셜 로그인**을 결합했고,
> **Redis를 활용해 Refresh Token**을 관리합니다.

---

### Q25. Access Token과 Refresh Token의 만료 시간을 다르게 둔 이유는?

> **A.**
> - **Access Token (1시간)** — 짧게 유지해 탈취 시 피해 최소화
> - **Refresh Token (7일)** — 길게 유지해 사용자가 자주 재로그인하지 않도록 UX 확보
>
> 보안과 사용성 사이의 균형을 맞춘 설계입니다.

---

### Q26. Refresh Token을 RDB가 아닌 Redis에 저장한 이유는?

> **A.**
> Redis는 저장 시 **TTL(만료 시간)을 설정**할 수 있어,
> 만료된 토큰을 개발자가 직접 검사하고 삭제하는 로직 없이 **자동으로 파기**되기 때문입니다.

---

### Q27. 로그아웃 시 토큰은 어떻게 무효화하나요?

> **A.**
> `AuthService.logout()`에서 해당 토큰의 **잔여 만료 시간을 계산해**
> `blacklist:{token}` 키로 Redis에 등록합니다.
> 이후 `JwtAuthenticationFilter`에서 매 요청마다 블랙리스트 여부를 확인해
> 만료 시간이 남아 있더라도 **즉시 사용이 불가능**하도록 처리합니다.

---

### Q28. 카카오 로그인(OAuth2) 흐름을 설명해 주세요.

> **A.**
> 1. 카카오로부터 **인가 코드** 수신
> 2. 인가 코드를 **카카오 액세스 토큰**으로 교환
> 3. 액세스 토큰으로 **사용자 정보 조회**
> 4. 신규·기존 회원 여부 판별 → **계정 생성 또는 갱신**
> 5. 자체 **JWT 발급** 후 응답

---

### Q29. 비밀번호는 어떻게 안전하게 저장하나요?

> **A.**
> `PasswordEncoder`를 통해 **단방향 해시로 암호화**해 저장합니다.
> 로그인 시에는 원문을 비교하는 것이 아니라 **해시값을 비교**하는 방식으로 검증합니다.

---

### Q30. `JwtAuthenticationFilter`가 토큰 검증에 실패해도 체인을 계속 진행시키는 이유는?

> **A.**
> `reissue`·`logout` 같은 일부 경로는 `shouldNotFilter`로 필터 자체를 건너뛰게 하고,
> 그 외에는 토큰이 없거나 유효하지 않아도 예외로 체인을 끊지 않고
> **SecurityContext만 비운 채 다음으로 넘깁니다.**
> 이렇게 해야 `permitAll`로 열어둔 공개 API(회원가입·로그인 등)까지 함께 막히는 것을 방지할 수 있습니다.
>
> #### └ 꼬리질문. 그러면 보호된 API에 만료된 토큰으로 접근하면 어떤 응답이 오나요?
>
> **A.**
> SecurityContext가 비어 있어 인증되지 않은 사용자로 처리되고,
> `exceptionHandling`에 등록한 **`AuthenticationEntryPoint`가 동작해 401 Unauthorized**가 반환됩니다.

---

### Q31. 액세스 토큰은 헤더와 쿠키 중 어떤 방식으로 검증하나요?

> **A.**
> `JwtAuthenticationFilter`의 `resolveToken()`이
> **`Authorization: Bearer` 헤더를 먼저 확인**하고,
> 없으면 **`accessToken` 쿠키를 확인**하는 순서로 두 방식을 모두 지원합니다.
> 이를 통해 REST 클라이언트와 브라우저 쿠키 방식 모두 호환됩니다.

---

### Q32. OAuth 로그인 성공 후 바로 쿠키를 심지 않고 코드 교환(exchange) 절차를 거치는 이유는?

> **A.**
> 카카오 OAuth2 콜백을 받는 **백엔드 도메인**과, 실제 쿠키가 필요한 **프론트엔드 도메인**이 서로 달라서
> 백엔드가 임의로 프론트 도메인에 쿠키를 심을 수 없기 때문입니다.
> 그래서 로그인 성공 시 Redis에 **60초 TTL의 1회성 코드**만 발급해 프론트로 리다이렉트하고,
> 프론트가 그 코드를 자체 API(`/api/v1/auth/oauth/exchange`)로 교환해 쿠키를 직접 설정합니다.
>
> #### └ 꼬리질문. 그 코드가 탈취되면 위험하지 않나요?
>
> **A.**
> TTL을 **60초로 매우 짧게** 두고, 교환 시 Redis에서 **즉시 삭제**되는 1회성 코드이기 때문에
> 탈취되더라도 유효 시간과 재사용 가능성이 극히 제한적입니다.

---

### Q33. 로그아웃된 토큰의 블랙리스트는 언제까지 유지되나요?

> **A.**
> 로그아웃 시 **해당 토큰에 남아 있는 잔여 만료 시간만큼** `blacklist:{token}` 키에 TTL을 걸어두기 때문에,
> 토큰이 원래 만료됐을 시점에 Redis에서도 **자동으로 함께 삭제**되어
> 별도 정리 로직이 필요 없습니다.

---

### Q34. 회원가입 흐름을 단계별로 설명해 주세요.

> **A.**
> 1. `POST /api/v1/auth/signup` — `loginId`, `password`, `nickname` 수신
> 2. `loginId` / `nickname` 중복 여부 DB 조회 → 중복 시 **409 Conflict** 응답
> 3. `passwordEncoder.encode(password)`로 **단방향 해시 암호화** 후 User 저장
> 4. 저장 즉시 **Access Token + Refresh Token 발급** (`signupAndLogin`)
> 5. Redis에 `refresh:{userId}` 키로 Refresh Token 저장 (7일 TTL)
> 6. `Rq.setCookie()`로 **HttpOnly 쿠키**에 두 토큰 세팅 후 사용자 프로필 응답
>
> 회원가입 완료 즉시 로그인 상태가 되므로 별도 로그인 요청이 필요 없습니다.

---

### Q35. 일반 로그인 흐름을 단계별로 설명해 주세요.

> **A.**
> 1. `POST /api/v1/auth/login` — `loginId`, `password` 수신
> 2. `UserRepository.findByLoginId()`로 유저 조회 → 없으면 **401** ("아이디/비밀번호가 올바르지 않습니다")
> 3. `passwordEncoder.matches(rawPassword, encodedPassword)`로 비밀번호 검증 → 불일치 시 **401**
> 4. **Access Token** (1시간 TTL) + **Refresh Token** (7일 TTL) 생성
> 5. Redis에 `refresh:{userId}` 키로 Refresh Token 저장
> 6. `accessToken`, `refreshToken`을 **HttpOnly 쿠키**로 응답
>
> #### └ 꼬리질문. "아이디가 틀렸습니다" / "비밀번호가 틀렸습니다"로 구분해 알려주지 않는 이유는?
>
> **A.**
> 어느 쪽이 틀렸는지 알려주면 공격자가 **아이디 존재 여부를 탐색**할 수 있기 때문입니다.
> 두 경우 모두 동일한 메시지로 응답해 **사용자 열거 공격(User Enumeration Attack)을 방어**합니다.

---

### Q36. 로그아웃 흐름을 단계별로 설명해 주세요.

> **A.**
> 1. `POST /api/v1/auth/logout` — **`JwtAuthenticationFilter`를 건너뜀** (`shouldNotFilter`)
> 2. `Rq.getCookieValue("accessToken")`으로 쿠키에서 토큰 추출
> 3. `JwtUtil.getRemainingExpiration(token)`으로 **잔여 만료 시간 계산**
> 4. `redisTemplate.set("blacklist:{token}", "logout", 잔여시간)` — Redis 블랙리스트 등록
> 5. `Rq.delCookie("accessToken")` + `Rq.delCookie("refreshToken")` — **쿠키 삭제**
>
> #### └ 꼬리질문. 쿠키 삭제만 해도 로그아웃 아닌가요? 왜 블랙리스트까지 필요한가요?
>
> **A.**
> 쿠키는 서버가 브라우저에 "삭제 요청"을 보내는 것이지, **토큰 자체를 무효화하는 것이 아닙니다.**
> 탈취된 Access Token이 있다면 쿠키가 없어도 `Authorization` 헤더에 담아 요청이 가능합니다.
> Redis 블랙리스트에 등록해야 **토큰 자체의 유효성을 즉시 차단**할 수 있습니다.

---

### Q37. 토큰 재발급(reissue) 흐름을 단계별로 설명해 주세요.

> **A.**
> 1. `POST /api/v1/auth/reissue` — **`JwtAuthenticationFilter`를 건너뜀** (`shouldNotFilter`)
> 2. `Rq.getCookieValue("refreshToken")`으로 쿠키에서 Refresh Token 추출
> 3. `JwtUtil.getUserId(refreshToken)`으로 **userId 파싱**
> 4. Redis에서 `refresh:{userId}` 조회 → 저장된 토큰과 **일치 여부 검증**
>    - 불일치 또는 없음 → **401** ("유효하지 않은 refreshToken입니다")
> 5. **새 Access Token + 새 Refresh Token 발급** (Refresh Token Rotation)
> 6. Redis에 새 Refresh Token **덮어쓰기** — 이전 Refresh Token 자동 무효화
> 7. 새 쿠키로 응답
>
> #### └ 꼬리질문. reissue를 `shouldNotFilter`로 JWT 필터에서 빼는 이유는?
>
> **A.**
> reissue를 요청하는 시점은 **Access Token이 이미 만료된 상황**입니다.
> 필터가 만료된 Access Token을 파싱하려 하면 `JwtException`이 발생해 SecurityContext가 비게 되고,
> 실제 로직과 무관하게 필터 레이어에서 오류가 날 수 있습니다.
> reissue 엔드포인트는 **Refresh Token만 검증**하면 되므로 JWT 필터를 건너뛰도록 설계했습니다.

---

### Q38. JWT Access Token에 어떤 정보를 담았나요?

> **A.**
> ```
> subject  : userId (Long → String)
> claims   : loginId, role, tokenType="access"
> issuedAt : 발급 시각
> expiration: 발급 시각 + accessExpiration
> signature: HMAC-SHA (HS256), Base64 decoded secret key
> ```
> Refresh Token은 `subject(userId)`와 `tokenType="refresh"`만 담아 최소화했습니다.
> 필터에서 `tokenType` 클레임으로 Access/Refresh를 구분해 **Refresh Token으로 API 직접 호출을 방지**합니다.
>
> #### └ 꼬리질문. userId 외에 loginId, role을 Access Token에 담은 이유는?
>
> **A.**
> 매 요청마다 DB를 조회하지 않아도 **토큰 자체에서 사용자 정보를 읽을 수 있도록** 하기 위함입니다.
> 단, role이 변경될 경우 기존 토큰에는 반영되지 않으므로, 권한 변경이 잦은 시스템이라면
> DB 조회 방식이 더 안전합니다.

---

### Q39. 토큰을 쿠키로 내려주는데 XSS, CSRF 공격은 고려했나요?

> **A.**
> - **XSS 방어** — `HttpOnly: true` 설정으로 JavaScript에서 쿠키에 접근 불가
> - **CSRF 방어** — `SameSite: Lax`(기본값) 설정으로 외부 사이트 요청 시 쿠키 미전송.
>   배포 환경에서 프론트/백 도메인이 다를 경우 `SameSite=None; Secure=true`로 전환 필요
> - **Secure 속성** — `cookie.secure` 설정으로 환경별 제어 (로컬: false, 배포: true)
>
> `Rq.setCookieValue()`에 이 속성들이 일괄 적용되어 **모든 쿠키가 동일한 보안 수준**을 가집니다.

---

### Q40. `Rq` 클래스를 별도로 만든 이유는?

> **A.**
> `HttpServletRequest`·`HttpServletResponse`를 컨트롤러마다 주입받으면 중복이 생깁니다.
> `Rq`를 **request-scoped 헬퍼**로 두어 쿠키 읽기(`getCookieValue`)·쓰기(`setCookie`)·삭제(`delCookie`)를
> 한 곳에서 관리하고, `HttpOnly`, `Secure`, `SameSite` 속성도 **중앙에서 일관되게** 적용합니다.

---

## 🚀 배포

---

### Q34. 배포를 지금처럼(Vercel + EC2) 선택한 이유는?

> **A.**
> - **Vercel** — 프론트엔드를 빠르고 간편하게 배포
> - **EC2** — 백엔드 서버를 직접 운영해보는 실무 경험 확보
>
> 프로젝트 초기에는 기능 동작이 우선이라, 설정이 간편한 플랫폼도 함께 고려했습니다.

---

### Q35. 왜 프론트와 백엔드를 서로 다른 플랫폼에 배포했나요?

> **A.**
> - **Next.js ↔ Vercel** — 궁합이 좋아 배포·프리뷰가 간편
> - **Spring Boot ↔ EC2** — 직접 관리형 서버에서 자유도 높게 운영 가능
>
> 각 레이어의 특성에 맞는 플랫폼을 선택했습니다.

---

### Q36. CI/CD 파이프라인은 어떻게 구성되어 있나요?

> **A.**
> **Docker**로 컨테이너화하고 **GitHub Actions**를 활용했으며,
> 이후 **Jenkins와 연동**해 `dev` 브랜치 커밋을 트리거로 빌드·배포까지 자동화하는 것을 계획하고 있습니다.

---

### Q37. 배포 환경에서 실제로 겪은 이슈가 있나요?

> **A.**
> 배포된 프론트엔드에서 로그인·회원가입 API 호출 시 **403 "Invalid CORS request"** 오류가 발생했습니다.
> 원인은 브라우저의 CORS 정책이 아니라 **백엔드 CORS 필터의 허용 Origin 설정 문제**였고,
> 이를 점검·수정하며 개발 환경과 운영 환경의 차이를 직접 경험했습니다.

---

### Q38. 트래픽이 몰릴 경우 어떻게 대응할 계획인가요?

> **A.**
> 현재는 단일 서버로 운영 중입니다.
> 향후 **컨테이너를 여러 개 띄우고 API Gateway를 통한 로드밸런싱**을
> 인프라 레벨에서 도입하는 것을 검토하고 있습니다.

---

### Q39. CORS 오류의 정확한 원인이 코드상 어디였나요?

> **A.**
> `SecurityConfig`의 `corsConfigurationSource()`가
> `app.frontend.url` 프로퍼티 값을 `allowedOrigins`에 추가하는 구조인데,
> **배포 환경변수에 설정된 값이 실제 Vercel 배포 도메인과 달라서** 차단됐습니다.
> 값을 실제 도메인과 일치시켜 해결했습니다.
>
> #### └ 꼬리질문. 로컬 개발환경에서는 왜 같은 문제가 없었나요?
>
> **A.**
> `dev` 프로파일의 `SecurityFilterChain`은 `anyRequest().permitAll()`로 전체 허용이라
> CORS 설정값과 무관하게 통과되고,
> `prod`(`!dev`) 프로파일에서만 `allowedOrigins` 검증이 실제로 걸리기 때문에
> **배포 이후에야 드러난 문제**였습니다.

---

### Q40. dev/prod 프로파일을 나눈 이유와 각각 어떻게 다른가요?

> **A.**
> - **dev** — `anyRequest().permitAll()`로 전부 열어두고, 개발 생산성 확보
> - **prod (`!dev`)** — 인증 없이 접근 가능한 경로만 화이트리스트로 명시하고, 나머지는 인증 요구
>
> 로컬 개발 편의와 운영 보안을 동시에 확보하는 전략입니다.

---

### Q41. Spring Boot 4.1.0, Java 25 같은 최신 버전을 그대로 써도 괜찮나요?

> **A.**
> **최신 스펙 학습과 실무 트렌드 반영**을 목적으로 채택했고,
> 실제 사용 범위(REST, JPA, Security, Redis) 안에서는 호환성 문제가 없었습니다.
> 다만 상용 서비스라면 **LTS 버전 대비 검증 기간이 짧다는 리스크**가 있다는 점도 인지하고 있습니다.

---

### Q42. EC2를 백엔드 서버로 선택한 구체적인 이유는 무엇인가요?

> **A.**
> 몇 가지 이유가 있습니다.
> - **실무와 유사한 환경** — Linux 서버에서 직접 포트 개방, 방화벽 설정, 프로세스 관리를 경험하기 위함
> - **자유도** — PaaS(Heroku, Railway 등)는 설정이 추상화되어 있어 내부 동작을 배우기 어렵지만,
>   EC2는 OS 레벨부터 직접 제어 가능
> - **Docker 운영 경험** — 컨테이너를 직접 빌드·배포·재시작하는 과정을 실습
> - **비용** — 프리티어(t2.micro / t3.micro) 활용으로 부담 없이 운영 가능

---

### Q43. EC2에서 Spring Boot 애플리케이션을 어떻게 실행하나요?

> **A.**
> **Docker 컨테이너**로 실행합니다.
> `docker-compose.yaml`에 Spring Boot 앱, MySQL, Redis 서비스를 정의해두고
> `docker compose up -d`로 한 번에 기동합니다.
> 애플리케이션 코드가 변경되면 새 이미지를 빌드해 컨테이너를 교체하는 방식으로 배포합니다.
>
> #### └ 꼬리질문. EC2가 재시작되면 컨테이너도 자동으로 올라오나요?
>
> **A.**
> `docker-compose.yaml`에 `restart: unless-stopped` 옵션을 설정하면
> EC2가 재부팅되어도 Docker 데몬이 뜨면서 **컨테이너가 자동으로 재시작**됩니다.

---

### Q44. EC2의 보안 설정(Security Group)은 어떻게 구성했나요?

> **A.**
> - **인바운드** — SSH(22)는 특정 IP에서만 허용, HTTP(80)·HTTPS(443)·앱 포트(8080)는 전체 허용
> - **아웃바운드** — 전체 허용
> - DB 포트(3306), Redis 포트(6379)는 **외부에 열지 않고** EC2 내부(Docker 네트워크)에서만 통신
>
> 민감한 포트는 외부에 노출하지 않아 직접 접근을 차단합니다.

---

### Q45. JWT secret이나 DB 비밀번호 같은 민감한 설정값은 어떻게 관리하나요?

> **A.**
> `application.yaml`에 직접 하드코딩하지 않고,
> **환경변수**로 주입받는 방식을 사용합니다.
> EC2에서는 `.env` 파일 또는 Docker `--env-file` 옵션으로 주입하고,
> `.gitignore`에 등록해 **Git에 커밋되지 않도록** 관리합니다.
> (레포에는 `application.yaml.example`만 올려 구조를 공유합니다)
>
> #### └ 꼬리질문. 팀원끼리 환경변수 값은 어떻게 공유했나요?
>
> **A.**
> ⚠️ **실제 공유 방법(노션 비공개 페이지, Slack DM, 팀 비밀 저장소 등)을 팀에서 확인해 채워주세요.**

---

## 🧪 테스트 / 품질

---

### Q42. 테스트 커버리지는 어느 정도인가요?

> **A.**
> ⚠️ **발표 전 실제 커버리지 수치를 측정해 채워주세요.**
> (예: `./gradlew test jacocoTestReport` 후 `build/reports/jacoco/` 확인)

---

### Q43. Swagger는 개발 과정에서 어떻게 활용했나요?

> **A.**
> **API 계약을 먼저 정의해 문서화**함으로써,
> 프론트엔드와 백엔드가 서로의 구현을 기다리지 않고 **병렬로 개발**할 수 있는
> 인터페이스 역할을 하도록 활용했습니다.

---

### Q44. DB 부하 테스트는 어떤 도구와 시나리오로 진행했고, 결과는 어땠나요?

> **A.**
> ⚠️ **발표 전 실제 결과를 채워주세요.**
> (사용 도구, 동시 사용자 수, 응답 시간, 에러율 등)

---

### Q45. 테스트 환경(H2)과 운영 환경(MySQL)이 다른데 어떻게 신뢰성을 확보하나요?

> **A.**
> 테스트는 **H2 인메모리로 빠르게 실행**하되,
> DB 방언에 민감한 쿼리는 별도로 MySQL 기준 통합 검증을 병행하는 방식을 취했습니다.
> **완전히 동일한 동작을 보장하지는 못한다는 한계도 인지**하고 있습니다.

---

### Q46. Spotless 같은 코드 스타일 도구는 왜 도입했나요?

> **A.**
> PR마다 포맷 관련 리뷰 코멘트가 반복되는 것을 줄이고,
> **Git Hook 단계에서 자동으로 스타일을 통일시켜** 팀 전체 코드 컨벤션을 강제하기 위해 도입했습니다.

---

## 🔍 구조 관련 심화 질문

---

### Q47. Redis를 캐시와 Refresh Token 저장, 두 용도로 함께 쓰고 있는데 인스턴스나 DB 번호를 분리했나요?

> **A.**
> ⚠️ **팀 논의 후 현재 구조와 향후 분리 계획을 채워주세요.**
> (예: 현재는 동일 인스턴스 사용, 향후 용도별 DB 번호 분리 또는 별도 인스턴스 운영 예정)

---

### Q48. 카카오 API 의존도가 높은데, 카카오 API 장애 시 로그인이나 검색 기능은 어떻게 되나요?

> **A.**
> 현재는 **대체 수단이 없다는 점을 인정**합니다.
> 향후 **서킷 브레이커 도입, 캐시된 데이터 폴백** 등 장애 격리 방안을 검토할 계획입니다.

---

### Q49. 알림 기능이 로드맵에 없는 것처럼 보이는데, 실제 구현 현황은 어떤가요?

> **A.**
> 실제로는 **Redis Streams 기반 알림 파이프라인이 이미 구현**되어 있습니다.
> 피드 생성 시 팔로워 전원에게 `NEW_FEED` 타입 알림이 DB에 저장되고,
> 사용자는 `GET /api/v1/notifications`로 알림을 조회하고 읽음 처리할 수 있습니다.
> 발표자료 업데이트가 누락된 부분이며 발표 시 정정할 예정입니다.

---

### Q50. 좋아요 수 카운트는 동시 요청 시 정확히 집계되나요?

> **A.**
> 현재는 **기본 기능 위주로 구현**되어 있어 동시성 이슈가 있을 수 있습니다.
> 향후 **메시지 큐 기반 순차 처리**로 해결할 예정입니다.

---

### Q51. 팔로잉 제외 후 무작위 셔플이 진짜 '개인화'라고 할 수 있나요?

> **A.**
> 현재는 **팔로우 관계 기반의 1차 필터링 단계**입니다.
> 향후 **좋아요·저장·방문 로그를 활용한 협업 필터링**과
> **임베딩 기반 추천**으로 고도화할 계획으로, 개인화 추천을 로드맵 최우선에 두고 있습니다.

---

### Q52. 프론트엔드와 백엔드를 분리한(모놀리식이 아닌) 구조를 택한 이유는?

> **A.**
> - 각 레이어를 **독립적으로 개발·배포·확장** 가능
> - 팀원들이 관심사에 따라(프론트 / 백엔드) **병렬로 작업** 가능
> - 향후 **스케일 아웃 시 각 레이어를 독립적으로 확장** 가능

---

### Q53. 8개 테이블로 구성된 ERD가 앞으로 기능이 늘어나도(DM, 알림 등) 확장 가능한 구조인가요?

> **A.**
> 핵심 엔티티(`users`, `restaurant`)를 중심으로 관계가 **정규화**되어 있어,
> 새 기능은 대부분 **새로운 테이블을 추가하고 기존 테이블에 FK로 연결**하는 방식으로 확장 가능합니다.
> 실제로 알림 엔티티도 이 방식으로 추가되었습니다.

---

### Q54. `WebClient`로 카카오 API를 호출할 때 타임아웃이나 장애 대응은 되어 있나요?

> **A.**
> 현재는 `baseUrl`만 지정한 기본 `WebClient` 설정이라
> **별도 타임아웃·재시도·서킷브레이커 로직은 없습니다.**
> 향후 **Resilience4j** 등을 도입해 카카오 API 장애 시
> 서비스 전체로 장애가 전파되지 않도록 격리할 계획입니다.

---

### Q55. Redis 하나로 Refresh Token, 블랙리스트, OAuth 코드, 알림 스트림까지 전부 처리하는데 부하가 몰리면 문제없나요?

> **A.**
> 현재는 **단일 Redis 인스턴스**로 모든 용도를 처리하고 있습니다.
> 트래픽이 커지면 **용도별로 DB 번호를 분리**하거나(세션용/스트림용),
> 필요 시 **별도 인스턴스로 나누는 것**을 검토할 계획입니다.

---

### Q56. 발표자료의 8개 테이블 ERD가 실제로 구현된 알림 기능까지 반영하고 있나요?

> **A.**
> 발표자료 ERD는 8개 테이블 기준으로 정리되어 있지만,
> **실제로는 알림 관련 엔티티(`Notification`)가 추가로 존재합니다.**
> ERD 문서화가 최신 구현을 완전히 따라가지 못한 부분이라,
> 발표 전 최신화하거나 질문 시 솔직하게 반영 예정이라고 답변하는 것이 좋습니다.

---

## 🔬 코드 기반 심화 질문

> 실제 코드를 읽고 발굴한 질문들입니다. 면접관이 코드를 직접 보고 물어볼 수 있는 수준입니다.

---

### Q57. `FeedEventListener`에서 `@TransactionalEventListener(phase = AFTER_COMMIT)`을 쓴 이유는?

> **A.**
> 피드 생성 트랜잭션이 **롤백될 경우 알림이 발행되지 않도록** 하기 위함입니다.
> `BEFORE_COMMIT`으로 발행하면 트랜잭션이 실패해도 Redis에 이벤트가 들어가 버릴 수 있습니다.
> 또한 Redis 발행 실패 시 예외를 catch해 삼키는 구조여서,
> **Redis 장애가 이미 커밋된 피드 생성 요청의 응답을 실패시키지 않도록** 설계되어 있습니다.

---

### Q58. 이미지를 로컬 파일시스템에 저장하는데, EC2가 재시작되면 이미지가 사라지지 않나요?

> **A.**
> 현재 `ImageUploadService`는 **서버 로컬 디렉토리(`uploads/`)에 파일을 저장**합니다.
> EC2 인스턴스를 교체하거나 컨테이너가 재생성되면 이미지가 유실될 수 있다는 한계가 있습니다.
> 향후 **AWS S3 같은 오브젝트 스토리지로 마이그레이션**하는 것을 계획하고 있습니다.

---

### Q59. Reissue 시 Refresh Token도 새로 발급(Rotation)하나요?

> **A.**
> 네, `AuthService.reissue()`에서 **새 Refresh Token을 생성해 Redis에 덮어씁니다.**
> 이 Refresh Token Rotation 방식은 이전 Refresh Token을 즉시 무효화하기 때문에,
> 탈취된 Refresh Token으로 무한 재발급하는 공격을 방어할 수 있습니다.

---

### Q60. 피드 목록 조회 시 댓글 수와 좋아요 여부를 각각 조회하는데 N+1 문제는 없나요?

> **A.**
> `FeedService.getFeeds()`에서 피드 목록을 가져온 뒤,
> 댓글 수는 `commentRepository.countByFeedIds(feedIds)` — **IN 쿼리 1번**,
> 좋아요 여부는 `feedLikeRepository.findLikedFeedIdsByUserIdAndFeedIds(userId, feedIds)` — **IN 쿼리 1번**
> 으로 처리합니다.
> 피드 수에 비례해 쿼리가 늘어나지 않아 **N+1 문제가 없는 구조**입니다.

---

### Q61. `RestaurantService`에서 `Random`을 필드로 공유하는데 멀티스레드 환경에서 안전한가요?

> **A.**
> `java.util.Random`은 thread-safe하지만, **경쟁 조건에서 성능이 저하**될 수 있습니다.
> 현재 수준의 트래픽에서는 문제없지만, 향후 **`ThreadLocalRandom.current()`로 교체**하면
> 스레드별로 독립된 인스턴스를 사용해 성능을 높일 수 있습니다.

---

### Q62. 리스트 복사 시 왜 `flush()`와 `entityManager.clear()`를 명시적으로 호출하나요?

> **A.**
> `RestaurantListService.copyList()`에서 아이템을 복사한 뒤,
> **`flush()`로 변경사항을 DB에 즉시 반영**하고,
> **`entityManager.clear()`로 1차 캐시를 비워** 이후 `findByIdWithItems()`가
> 캐시가 아닌 DB에서 최신 상태를 조회하도록 강제합니다.
> 이 과정 없이는 JPA 1차 캐시에 아이템이 포함되지 않아 응답에 빠져 반환될 수 있습니다.

---

### Q63. `shouldNotFilter`에서 `reissue`, `logout` 경로를 필터에서 아예 제외하는 이유는?

> **A.**
> - **`reissue`** — 만료된 Access Token으로 요청하는 엔드포인트라, 필터에서 파싱하면 무조건 실패합니다.
> - **`logout`** — 블랙리스트 등록을 위해 raw token 값 자체가 필요한데,
>   필터가 먼저 파싱·인증해버리면 `AuthService.logout()`에 token 문자열을 넘기는 흐름이 어긋납니다.
>
> 두 경로는 Controller에서 직접 토큰을 받아 처리하도록 설계되어 있어 필터를 건너뜁니다.

---

### Q64. 이미지 업로드 시 파일명을 UUID로 바꿔 저장하는 이유는?

> **A.**
> 두 가지 목적이 있습니다.
> 1. **보안** — 원본 파일명에 `../` 같은 경로 문자가 포함될 경우 **Path Traversal 취약점**이 생길 수 있습니다.
>    (코드에서 `StringUtils.cleanPath()`로 1차 정리하고 UUID로 교체하는 2중 방어)
> 2. **충돌 방지** — 여러 사용자가 같은 파일명으로 업로드해도 **파일이 덮어쓰이지 않도록** 합니다.

---

> 💡 **TIP** — ⚠️ 표시된 항목은 발표 전에 팀에서 실제 수치나 결정 사항을 채워 완성해 주세요.
